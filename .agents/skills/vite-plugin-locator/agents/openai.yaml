interface:
  display_name: "Vite Plugin Locator"
  short_description: "Repo-specific patterns and workflows for vite-plugin-locator"
  default_prompt: "Use the vite-plugin-locator repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true